#include "c8sound.h"

void beep()
{
	/* As far as I know, UEFI has no sound interfaces by default, one
	   should use an external driver to make it work. So this is only
	   a stub. */
	return;
}